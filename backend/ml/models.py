# The ml app is intentionally model-less: clustering (clustering.py) and
# recommendation (recommendations.py) results are computed on the fly from
# existing Task/Course/User data rather than being stored, since they need
# to reflect the latest activity every time they're requested.
