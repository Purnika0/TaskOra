# Nothing to register here — the ml app has no models of its own.
# It's a computation-only app: clustering.py and recommendations.py operate
# directly on Task/User/Enrollment data owned by other apps.
